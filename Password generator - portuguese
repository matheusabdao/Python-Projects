import random
import string

def generate_password(length):
    senha = ''.join(random.choice(string.ascii_letters + string.digits) for i in range(length))
    return senha

tamanho_senha = 10
password = generate_password(tamanho_senha)
print("Sua senha gerada é: ", senha)
